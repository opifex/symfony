framework:
  workflows:
    account:
      type: state_machine
      supports:
        - App\Infrastructure\Workflow\AccountMarking
      initial_marking: created
      audit_trail:
        enabled: '%kernel.debug%'
      places:
        - activated
        - blocked
        - created
        - registered
      transitions:
        register:
          from: created
          to: registered
        activate:
          from: registered
          to: activated
        block:
          from: activated
          to: blocked
        unblock:
          from: blocked
          to: activated
